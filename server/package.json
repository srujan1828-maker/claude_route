{
  "name": "claude-bridge-server",
  "version": "1.0.0",
  "description": "OpenAI-compatible proxy server that bridges to claude.ai via Chrome Extension",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "cors": "^2.8.5"
  }
}
